export enum GameState { init, enter, run, jump, slip, die };

export enum SpeedDef {
    BG_MIDDLE_SPEED = 50,
    BG_NEAR_SPEED = 80,
    BG_RIVER_SPEED = 100,
    FLOOR_SPEED = 130,
};

export enum BuildType {
    start,
    middle1,
    middle2,
    end,
    high,
};
